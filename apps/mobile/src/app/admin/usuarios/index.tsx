import { AdminPlaceholder } from "@/components/AdminPlaceholder";

export default function AdminUsersScreen() {
  return (
    <AdminPlaceholder
      title="Usuarios"
      description="Roles, acceso administrativo y estado de cuentas."
    />
  );
}
