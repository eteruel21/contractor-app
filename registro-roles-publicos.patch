diff --git a/apps/mobile/src/app/(auth)/registro.tsx b/apps/mobile/src/app/(auth)/registro.tsx
index 0ab7742..7b15e27 100644
--- a/apps/mobile/src/app/(auth)/registro.tsx
+++ b/apps/mobile/src/app/(auth)/registro.tsx
@@ -19,7 +19,10 @@ import {
     colors,
     radius,
 } from "@/constants/theme";
-import { useAuth } from "@/contexts/AuthContext";
+import {
+  type PublicAppRole,
+  useAuth,
+} from "@/contexts/AuthContext";
 
 export default function RegisterScreen() {
   const { signUp } = useAuth();
@@ -32,6 +35,8 @@ export default function RegisterScreen() {
     useState("");
   const [confirmPassword, setConfirmPassword] =
     useState("");
+  const [accountRole, setAccountRole] =
+    useState<PublicAppRole>("contractor");
   const [submitting, setSubmitting] =
     useState(false);
 
@@ -79,6 +84,7 @@ export default function RegisterScreen() {
         phone,
         email,
         password,
+        role: accountRole,
       });
 
       if (error) {
@@ -140,10 +146,94 @@ export default function RegisterScreen() {
           </Text>
 
           <Text style={styles.subtitle}>
-            Registra al propietario principal de la empresa.
+            {accountRole === "contractor"
+              ? "Crea tu cuenta para administrar empresas, clientes y obras."
+              : "Crea tu acceso para consultar proyectos y presupuestos compartidos."}
           </Text>
 
           <View style={styles.card}>
+            <Text style={styles.accountTypeLabel}>
+              Tipo de cuenta
+            </Text>
+
+            <View style={styles.roleSelector}>
+              <Pressable
+                accessibilityRole="button"
+                accessibilityState={{
+                  selected: accountRole === "contractor",
+                }}
+                onPress={() => setAccountRole("contractor")}
+                style={({ pressed }) => [
+                  styles.roleOption,
+                  accountRole === "contractor" &&
+                    styles.roleOptionActive,
+                  pressed && styles.pressed,
+                ]}
+              >
+                <View style={styles.roleOptionHeader}>
+                  <Ionicons
+                    name="construct-outline"
+                    size={20}
+                    color={
+                      accountRole === "contractor"
+                        ? colors.primary
+                        : colors.textSecondary
+                    }
+                  />
+                  <Text
+                    style={[
+                      styles.roleOptionTitle,
+                      accountRole === "contractor" &&
+                        styles.roleOptionTitleActive,
+                    ]}
+                  >
+                    Contratista
+                  </Text>
+                </View>
+                <Text style={styles.roleOptionDescription}>
+                  Administra empresas, clientes y trabajos.
+                </Text>
+              </Pressable>
+
+              <Pressable
+                accessibilityRole="button"
+                accessibilityState={{
+                  selected: accountRole === "client",
+                }}
+                onPress={() => setAccountRole("client")}
+                style={({ pressed }) => [
+                  styles.roleOption,
+                  accountRole === "client" &&
+                    styles.roleOptionActive,
+                  pressed && styles.pressed,
+                ]}
+              >
+                <View style={styles.roleOptionHeader}>
+                  <Ionicons
+                    name="person-outline"
+                    size={20}
+                    color={
+                      accountRole === "client"
+                        ? colors.primary
+                        : colors.textSecondary
+                    }
+                  />
+                  <Text
+                    style={[
+                      styles.roleOptionTitle,
+                      accountRole === "client" &&
+                        styles.roleOptionTitleActive,
+                    ]}
+                  >
+                    Cliente
+                  </Text>
+                </View>
+                <Text style={styles.roleOptionDescription}>
+                  Consulta proyectos y presupuestos compartidos.
+                </Text>
+              </Pressable>
+            </View>
+
             <FormField
               label="Nombre completo"
               value={fullName}
@@ -369,6 +459,57 @@ const styles = StyleSheet.create({
     backgroundColor: colors.surface,
   },
 
+  accountTypeLabel: {
+    marginBottom: 9,
+    color: colors.text,
+    fontSize: 12,
+    fontWeight: "800",
+  },
+
+  roleSelector: {
+    flexDirection: "row",
+    gap: 10,
+    marginBottom: 20,
+  },
+
+  roleOption: {
+    flex: 1,
+    minHeight: 112,
+    padding: 12,
+    borderWidth: 1,
+    borderColor: colors.border,
+    borderRadius: radius.md,
+    backgroundColor: "#F8FAFC",
+  },
+
+  roleOptionActive: {
+    borderColor: colors.primary,
+    backgroundColor: colors.primarySoft,
+  },
+
+  roleOptionHeader: {
+    flexDirection: "row",
+    alignItems: "center",
+    gap: 7,
+  },
+
+  roleOptionTitle: {
+    color: colors.text,
+    fontSize: 13,
+    fontWeight: "900",
+  },
+
+  roleOptionTitleActive: {
+    color: colors.primaryDark,
+  },
+
+  roleOptionDescription: {
+    marginTop: 8,
+    color: colors.textSecondary,
+    fontSize: 11,
+    lineHeight: 15,
+  },
+
   field: {
     marginBottom: 16,
   },
diff --git a/apps/mobile/src/app/(client-tabs)/index.tsx b/apps/mobile/src/app/(client-tabs)/index.tsx
index 79dd06f..5e38e78 100644
--- a/apps/mobile/src/app/(client-tabs)/index.tsx
+++ b/apps/mobile/src/app/(client-tabs)/index.tsx
@@ -16,7 +16,7 @@ import { useAuth } from "../../contexts/AuthContext";
 import { getClientContractorCompanies } from "../../services/client-service";
 
 export default function ClientHomeScreen() {
-  const { profile, signOut } = useAuth();
+  const { profile, user, signOut } = useAuth();
   const [contractors, setContractors] = useState<any[]>([]);
   const [loading, setLoading] = useState(true);
 
@@ -90,7 +90,7 @@ export default function ClientHomeScreen() {
             <Text style={styles.emptyTitle}>Sin proveedores vinculados</Text>
             <Text style={styles.emptyText}>
               Pídele a tu contratista que registre tu correo electrónico (
-              {profile?.phone || "el que usaste al registrarte"}) para poder
+              {user?.email || "el que usaste al registrarte"}) para poder
               vincularte de forma automática.
             </Text>
           </View>
@@ -256,4 +256,3 @@ const styles = StyleSheet.create({
     color: colors.textSecondary,
   },
 });
-
diff --git a/apps/mobile/src/contexts/AuthContext.tsx b/apps/mobile/src/contexts/AuthContext.tsx
index 2cdf30b..7667749 100644
--- a/apps/mobile/src/contexts/AuthContext.tsx
+++ b/apps/mobile/src/contexts/AuthContext.tsx
@@ -17,6 +17,8 @@ import { supabase } from "@/services/supabase";
 
 export type AppRole = "super_admin" | "contractor" | "client";
 
+export type PublicAppRole = Exclude<AppRole, "super_admin">;
+
 export type AppProfile = {
   id: string;
   full_name: string | null;
@@ -33,7 +35,7 @@ type SignUpInput = {
   phone?: string;
   email: string;
   password: string;
-  role?: AppRole;
+  role?: PublicAppRole;
 };
 
 type AuthResult = {
@@ -298,4 +300,3 @@ export function useAuth(): AuthContextValue {
 
   return context;
 }
-

